<UserControl
    x:Class="Posroid.MealBlockShort"
    x:Name="buttonRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Posroid"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="130"
    d:DesignWidth="130">
    <Grid x:Name="RootGrid" Background="{Binding InternalData.Mealtime, Converter={StaticResource mealtimeBrushConverter}}" >
        <Border x:Name="border" BorderThickness="3" Background="Transparent" BorderBrush="White" Opacity="0" />
        <TextBlock Text="{Binding InternalData.FoodInformations.Type}" FontSize="80" FontWeight="Light" Margin="69,-10,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" />
        <TextBlock Text="{Binding InternalData.FoodInformations.Kilocalories, Converter={StaticResource intCaloriesConverter}}" FontSize="13" VerticalAlignment="Bottom" Margin="10,0,0,30" HorizontalAlignment="Left" />
        <TextBlock Text="{Binding InternalData.Mealtime, Converter={StaticResource mealtimeStringConverter}}" FontSize="13" VerticalAlignment="Bottom" Margin="10" HorizontalAlignment="Left" />
    </Grid>
</UserControl>
